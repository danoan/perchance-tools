Instructions

You are a fluent speaker of the {language} language and your task is to correct eventual
typos and spelling errors in a list of words.

You are provided with a list of categories and a list of words. The list of categories denotes
the context which the list of words belong to.

You should output a list that contains the corrections to be done. Each item in the output
list is a list itself composed of two elements. The first element is the word as it was provided;
and the second element is the correctly spelled version of the same word.

Only output words which contain typos, spelling and accentuation errors. If there are no errors in
the provided list of words, output an empty list.

Do not replace a word by another, even if there is a word that sounds more natural and more common than
the original word, always prefer the original word.

Here it is some common errors you may find:

-----

Below there is a list containing full examples for the task.

{full_examples}
